# Local Development Configuration
# This file contains environment variables for local development
# Copy this file to .env.local and modify the values as needed

# API Configuration for Local Development
# Base URL for the API endpoints (local development server)
VITE_API_BASE_URL=http://localhost:5173/api

# WebSocket base URL for real-time connections (local development server)
VITE_WS_BASE_URL=ws://localhost:5173/ws

# Application Configuration
# Name of the application (used for branding)
VITE_APP_NAME=StarterJam (Dev)

# The URL of the GitHub repository for the project
VITE_GITHUB_REPO=https://github.com/nicholasgriffintn/starterjam.com

# Environment identifier
VITE_ENVIRONMENT=development

# WebSocket Configuration
# Maximum number of reconnection attempts before giving up
VITE_MAX_RECONNECT_ATTEMPTS=3

# Base delay in milliseconds between reconnection attempts (shorter for dev)
VITE_RECONNECT_BASE_DELAY=500

# Maximum delay in milliseconds between reconnection attempts (shorter for dev)
VITE_MAX_RECONNECT_DELAY=5000

# Development-specific settings
# Enable debug logging (if your application supports it)
VITE_DEBUG=true

# Local database URL (if applicable)
# VITE_LOCAL_DB_URL=http://localhost:3000

# API Server Configuration (Local Development)
# For local development, these should be set in your local wrangler.jsonc or .dev.vars file

# Application Configuration (Local)
# APP_NAME=StarterJam API (Dev)
# ENVIRONMENT=development
# APP_VERSION=1.0.0-dev

# Room Configuration (Local - more permissive for testing)
# ROOM_KEY_LENGTH=4
# MAX_USERS_PER_ROOM=10
# ROOM_TIMEOUT_MINUTES=30
# MAX_ROOMS_PER_IP=10

# Security Configuration (Local - more permissive for development)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# RATE_LIMIT_MAX_REQUESTS=1000
# RATE_LIMIT_WINDOW_MS=60000
# JWT_SECRET=dev-secret-key

# WebSocket Configuration (Local - shorter intervals for testing)
# WS_HEARTBEAT_INTERVAL=10000
# WS_CONNECTION_TIMEOUT=30000
# MAX_CONNECTIONS_PER_ROOM=20

# API Internal Configuration (Local)
# API_DUMMY_BASE_URL=https://local-internal